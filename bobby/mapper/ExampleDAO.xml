<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--这里的namespace就是指的你要实现的dao层接口的类路径 -->
<mapper namespace="com.bobby.dao.ExampleDAO">

	<select id="select" resultType="com.bobby.dao.ExampleDTO">
		select * from users
	</select>

	<insert id="insert">
		insert into test_table values(16,'tongjie','123','cq',0)
	</insert>
</mapper>